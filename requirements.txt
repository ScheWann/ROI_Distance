# Enhanced ROI Distance Generator and Visualizer
# Required Python Packages

# Core Dependencies
pandas>=1.3.0
numpy>=1.19.0
PyQt5>=5.15.0
matplotlib>=3.3.0
pydicom>=2.2.0
scipy>=1.6.0
scikit-image>=0.18.0
openpyxl>=3.0.0

# Optional: GPU Support (uncomment the line matching your CUDA version)
# For CUDA 11.x:
# cupy-cuda11x>=11.0.0
# For CUDA 12.x:
# cupy-cuda12x>=12.0.0

# Note: seaborn is NOT required and may cause startup hangs on some systems
# The tool works without it
